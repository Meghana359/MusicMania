<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.musicmania">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".charliePuthSongs"
            android:label="Charlie Puth"></activity>
        <activity
            android:name=".marron5Songs"
            android:label="Marron5"></activity>
        <activity
            android:name=".taylorSwiftSongs"
            android:label="Taylor Swift"></activity>
        <activity
            android:name=".currentSong"
            android:label="Now Playing"></activity>
    </application>

</manifest>